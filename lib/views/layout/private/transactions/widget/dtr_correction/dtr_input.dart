import 'package:flutter/material.dart';

class DTRInput extends StatelessWidget {
  const DTRInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
